import React from 'react';
import styles from './app.module.scss';

const App: React.FC = () => (
  <div className={styles.app}>Text template</div>
);

export default App;

/* if hooks: use => {
* here hooks
* return (....)
* } */
